(defpoll time :interval "10s"
  "date '+%H:%M %b %d, %Y'")

(defwidget time []
    (label :text {time} :class "_${ws}")
)

(defwidget time-wayland []
    (label :text {time} :style "color: #eba0ac;" :visible bar-visibility)
)