(defpoll time :interval "10s"
  "date '+%H:%M %b %d, %Y'")

(defwidget time []
    (label :text {time}
           :visible bar-visibility 
           :class "_${ws}"
           :style "color: #eba0ac;"
           ;; :class "_${current_workspace}";
    )
)