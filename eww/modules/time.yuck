(defpoll time :interval "10s"
  "date '+%H:%M %b %d, %Y'")

(defwidget time []
    (label :text {time} :class "_${ws}")
)

(defwidget time-wayland []
    (label :text {time} :class "_${current_workspace}" :visible bar-visibility)
)